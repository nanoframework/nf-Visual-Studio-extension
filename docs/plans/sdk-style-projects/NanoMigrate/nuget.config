<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- This tool has no NuGet dependencies; clearing sources lets it restore and
       build with no network access. Remove this file if you add PackageReferences. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
